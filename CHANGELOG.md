# 更新日志

本文件记录 fadb 桌面应用每个版本的可见变化,格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循语义化版本。每个版本对应仓库里的 `v*` 标签;更早的零散发布(BridgeScope 时期 0.4.x—0.6.x)不再逐条回填,可在 [Releases 页面](https://github.com/yeqing17/fadb/releases)按标签查看。

## [未发布]

### 新增

- 设置窗口与 ADB 诊断窗口新增「官方下载」入口,一键打开 Android 平台工具(platform-tools)下载页;中文界面使用 Google 中国镜像域名,英文界面使用 developer.android.com。
- `FADB_ADB` 环境变量可直接指定 `adb` 可执行文件路径,优先级高于 `PATH`、`ANDROID_SDK_ROOT` 与 `ANDROID_HOME`;指向不存在的文件时会在界面明确报错,不再静默回退到自动查找。

### 修复

- 水平行内按钮文字与纯文本标签的基线不再错位(设置页 ADB 信息行、官方下载行等;此前按钮文字低 4 像素)。
- 中文字体改为主字体而非回退字体,「AI 助手」等中西文混排标签与相邻纯中文标签共享同一条基线,不再有 1 像素偏移。
- 顶栏设备选择器的左边缘与左侧导航面板边界对齐;左侧导航收窄至 150 点。
- 设置窗口「外观 / ADB / 关于」三个分组标题恢复正文字号,三个分组第二列起始位置一致。
- 中央面板的错误横幅按语言显示标题,不再直接输出内部键名;左侧导航项 WebView 中文化为「网页」。

## [0.8.3] - 2026-08-31

### 新增

- 网络设备一键断开:设备管理器为已连接的网络端点提供「断开」按钮。
- 设置窗口 ADB 信息的「详情 / 收起」浮动窗口:展示可执行文件路径、ADB 版本与检测到的设备数。

## [0.8.2] - 2026-08-31

### 新增

- 设置窗口:外观(浅色/深色主题)、界面语言(中文/English)、ADB 检测信息(可执行文件、版本、设备数)与关于区块,入口在顶栏齿轮按钮。

## [0.8.1] - 2026-08-31

### 新增

- fadb 品牌 logo:窗口图标、可执行文件图标与应用内标题栏标识。

### 修复

- AI 助手不再杜撰不存在的 `fadb` 命令行工具,系统提示词与文档同步修正。

## [0.8.0] - 2026-08-30

### 新增

- scrcpy 投屏遥控:按键行遥控设备,一键把当前画面录成 MP4。
- 终端快捷指令:可编辑的命令按钮栏,支持本地持久化与 JSON 导入/导出。

### 变更

- 项目由 BridgeScope 更名为 fadb。

## [0.7.1] - 2026-08-30

### 移除

- 移除 0.6.0 引入的 AVD 管理器面板。

## [0.7.0] - 2026-08-30

### 新增

- scrcpy 投屏:转发隧道显示设备画面(仅视频,可调最大尺寸/码率)。
